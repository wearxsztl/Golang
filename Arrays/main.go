package main

import (
	"fmt"
	// "sort"
	//"math"
)

func main() {
	//var names [3]string
	//	names[0] = "Jordan"

	// names := [3]string{"Jardan", "John", "Kate"}
	// fmt.Println(names, len(names))
	// for i := 0; i < len(names); i++ {
	// 	fmt.Println(names[i])
	// }

	//////////////////////////////////Ср Арефм
	// var sum float64
	// marks := []float64{11, 9, 12, 8, 2, 10}
	// for i := 0; i < len(marks); i++ {
	// 	sum += marks[i]
	// }
	// var sredAref float64 = sum / float64(len(marks))
	// fmt.Println(math.Round(sredAref))

	///////////////Многомерный массив
	// array := [3][4]int{{1, 2, 3}, {4, 5, 6}, {7, 8, 9}}
	// fmt.Println(array)
	// fmt.Println(array[1][2])

	//////////////////Срезы - динамический массив
	// slice := []int{3, 1, 2, 5, 7}
	// sliicestr := []string{"e", "v", "c", "b", "a"}
	// slice = append(slice, 0) // добавляет элемент в конец массива
	// slice[0] = 11
	// sort.Ints(slice)        //сортирует по порядку
	// sort.Strings(sliicestr) //сортирует стринговый массив
	// sliicestr = append(sliicestr, "h")
	// fmt.Println(slice)
	// fmt.Println(sliicestr)

	//////////////////////Range

	// slice := []int{1, 5, 6, 3, 7, 9}
	// for i := 0; i < len(slice); i++ {
	// 	fmt.Println(slice[i])
	// }

	// for i, element := range slice {
	// 	fmt.Printf("%d: %d\n", i, element)
	// }
	// //or
	// // for _, element := range slice {
	// // 	fmt.Printf("%d\n", element)
	// // }

}
